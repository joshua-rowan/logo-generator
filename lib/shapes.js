// shapes parent class
class Shape {
    // all shapes will take on color parameter
    constructor(color) {
        this.color = color;
    }

    render () {
        // to be passed on and filled out by child classes
    }
};
